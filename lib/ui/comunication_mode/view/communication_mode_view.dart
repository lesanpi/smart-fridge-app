import 'package:flutter/material.dart';

class CommunicationModeView extends StatelessWidget {
  const CommunicationModeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class StandaloneCommunicationView extends StatelessWidget {
  const StandaloneCommunicationView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class CoordinatorCommunicationView extends StatelessWidget {
  const CoordinatorCommunicationView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
